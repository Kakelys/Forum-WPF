Нужна проверка на то, существует ли строка("v:String") синхронизируемого файла в главном,
если токового быть не должно, то он должен удаляться из синхронизируемого,
P.s. А то из-за этого после лишней строки добавляются всё остальное из главного файла